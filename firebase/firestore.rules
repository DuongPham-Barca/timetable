rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development-only rules for the current no-login app.
    // Replace these with Firebase Auth-based rules before publishing publicly.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
